/**
 * Full-viewport ambient layers (files in /public). With 2+ paths, layers crossfade
 * on a timer and when you move to a new presentation section.
 */
export const PRESENTATION_BACKGROUNDS: string[] = [
  "/background.png",
  // e.g. "/poker-night.jpeg", "/grid-line.png"
];
